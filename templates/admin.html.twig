<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <title>{% block title %}Administration!{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/axentix@1.3.3/dist/css/axentix.min.css">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% block stylesheets %}
        <!-- Axentix JS minified version -->
        <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css" integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous"/>
    {% endblock %}    

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/axentix@1.3.3/dist/js/axentix.min.js" defer></script>
    {% endblock %}
    <title>Document</title>
</head>
<body>
    <header>
        <!-- Menu fixe du haut -->
         <div class="container-fluid" id="bandeau">              
             <div class="container">
             <a href="{{ path('home') }}"><video id="logo" class="circle" autoplay="on" src="{{ asset('images/video.mp4') }}" height="250px" width="250px">test</video></a> 
                 <div class="insta">
                     <a href="https://www.instagram.com/cecilia_or_coaching" target="_blank" title="suivez-moi sur Instagram"><i class="fab fa-instagram"></i></a>
                     <a href="#" title="suivez-moi sur Linkedin"><i class="fab fa-linkedin"></i></a>
                 </div>
                 <ul class="text-center menu">
                     <li class="nav-item"><a class="nav-link" href="{{ path('home') }}#index">Le coaching</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('home') }}#titre">Qui suis-je ?</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('home') }}#coaching">Prestations</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('tarifs') }}">Les tarifs</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('contact') }}">Contact</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('actus') }}">Actualités</a></li>
                     <li class="nav-item"><a class="nav-link" href="{{ path('commentaires') }}">Les avis</a></li>
                 </ul>
             </div>

         </div>
         
     </header>
    {# <div id="example-sidenav" data-ax="sidenav" class="sidenav shadow-1 large fixed white">
        <div class="sidenav-header">
            <button data-target="example-sidenav" class="sidenav-trigger"><i class="fas fa-times"></i></button>
        </div>
        {% if app.user %}
             {% if is_granted('ROLE_ADMIN') %}
                <a href="{{path('admin_actus')}}" class="sidenav-link">Liste des actualités</a>
                <a href="{{path('formules')}}" class="sidenav-link">Liste des formules</a>
                <a href="{{path('formules')}}" class="sidenav-link">Liste des supports</a>
            {% endif %}
        {% endif %}
    </div> #}
  
    <main> 
        {% block body %}{% endblock %}
    </main>

    <footer class="footer airforce dark-1">
        <p class="m-0 text-center text-white">Copyright &copy; Site de Cécilia Orsi commencé le 04/06/2021</p>
        <p class="m-0 text-center text-white">Réalisé par Michel Biderman</p>
    </footer>
    

    <!-- Axentix JS minified version -->
    <script src="https://cdn.jsdelivr.net/npm/axentix@1.3.3/dist/js/axentix.min.js"></script>
</body>
</html>
</body>
</html>